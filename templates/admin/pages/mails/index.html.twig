{% extends 'admin/pages/mails/template.html.twig' %}

{% set page_title %}Boite mail{% endset %}
{% set page_subtitle %}Boite mail configuré en IMAP.{% endset %}

{% block main_content %}
    <div class="main-content">
        <div id="mails"
             data-total-s="{{ pagination.getTotalItemCount }}"
             data-total-t="{{ totalTrash }}"
             data-donnees="{{ data }}"
             data-from="{{ from }}"
             data-from-name="{{ fromName }}"
        >
            {% include "layout/components/loader.html.twig" %}
        </div>

        <div class="navigation">
            {{ knp_pagination_render(pagination) }}
        </div>
    </div>
{% endblock %}
